{
    "name": "msh-backend",
    "version": "1.0.0",
    "private": true,
    "scripts": {
          "dev": "wrangler dev",
          "deploy": "wrangler deploy",
          "build": "tsc",
          "db:init": "wrangler d1 execute msh-stock-db --file=schema.sql",
          "db:local": "wrangler d1 execute msh-stock-db --local --file=schema.sql"
    },
    "dependencies": {},
    "devDependencies": {
          "@cloudflare/workers-types": "^4.20240529.0",
          "typescript": "^5.4.0",
          "wrangler": "^3.57.0"
    }
}
